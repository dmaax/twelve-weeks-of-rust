{
  "name": "worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}